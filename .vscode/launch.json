{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Attach to Docker Container",
      "type": "python",
      "request": "attach",
      "connect": {
        "host": "localhost",
        "port": 8000
      },
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}",
          "remoteRoot": "/app"
        }
      ]
    }
  ]
}